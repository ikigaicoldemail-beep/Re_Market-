# ReMarket Marketplace Backend — Remaining Tasks

> Generated by QA audit on 2026-05-30.  
> Sorted by priority. Estimated effort uses T-shirt sizing: XS <1h, S 1–4h, M 4–8h, L 1–3d, XL 3d+.

---

## PRIORITY 1 — CRITICAL (blocks production)

| # | Task | File(s) | Effort |
|---|------|---------|--------|
| 1 | **Fix `CartService::updateItem()` variant bug** — method checks `$product->stock_quantity` and uses `$product->price_amount` even when the cart item has a variant; must check `$variant->stock_quantity` and use `$variant->price_amount` | `app/Services/CartService.php:107-128` | S |
| 2 | **Integrate a real payment gateway** — checkout creates a `manual/pending` payment record with no actual money movement; need Stripe / ABA Pay / PayPal SDK, initiate-payment endpoint, and webhook handler for confirmation/failure | `app/Services/CheckoutService.php`, `app/Http/Controllers/Api/V1/PaymentController.php`, new `app/Services/PaymentService.php` | XL |
| 3 | **Add webhook controller for payment callbacks** — no handler exists for payment gateway `POST` callbacks (Stripe `payment_intent.succeeded`, etc.) | new `app/Http/Controllers/Api/V1/PaymentWebhookController.php` | L |
| 4 | **Validate `provider` and `payment_method` against allowed enum values** — `CheckoutRequest` accepts any arbitrary string; must use `Rule::in([...])` | `app/Http/Requests/Checkout/CheckoutRequest.php:18-19` | XS |

---

## PRIORITY 2 — HIGH (significant feature gaps)

| # | Task | File(s) | Effort |
|---|------|---------|--------|
| 5 | **Implement discount/coupon code system** — `discount_amount` column exists on `orders` and `carts` but no validation logic, no coupon table, no service; hardcoded to 0 | `app/Services/CheckoutService.php:92`, new migration + model + service | L |
| 6 | **Implement shipping cost calculation** — `shipping_amount` hardcoded to 0; need at minimum a flat-rate or weight-based rule | `app/Services/CheckoutService.php:93`, `app/Services/CartService.php:154` | M |
| 7 | **Add max quantity cap in `AddToCartRequest`** — no upper bound allows requests for millions of units (service stock check is a DB round-trip; validation should reject early) | `app/Http/Requests/Cart/AddToCartRequest.php:17` | XS |
| 8 | **Add `Rule::in` validation for `payment_method` field** — related to item 4 above | `app/Http/Requests/Checkout/CheckoutRequest.php` | XS |
| 9 | **Enforce email verification before marketplace operations** — `AuthService` sets `status = active` immediately; `email_verified_at` column exists but is never checked; should block cart/checkout/store creation until email verified | `app/Services/AuthService.php:34`, `app/Http/Middleware/EnsureAccountIsActive.php` | M |
| 10 | **Send transactional emails** — no order confirmation email sent to buyer/seller after checkout, no password-reset email is sent (routes exist but `Mail` classes likely missing) | new `app/Mail/OrderPlacedMail.php`, `app/Mail/PasswordResetMail.php` | M |

---

## PRIORITY 3 — MEDIUM (quality & completeness)

| # | Task | File(s) | Effort |
|---|------|---------|--------|
| 11 | **Strengthen password rules** — `Password::min(8)->letters()->numbers()` has no symbol requirement; add `->symbols()` and consider raising minimum to 10 characters | `app/Http/Requests/Auth/RegisterRequest.php:22`, `app/Http/Requests/Auth/ResetPasswordRequest.php` | XS |
| 12 | **Write tests for Cart operations** — `CartController` has zero test coverage; add add-item, update-item (variant path), remove-item, clear | `tests/Feature/Api/V1/CartApiTest.php` (new) | M |
| 13 | **Write tests for Order management** — `OrderController` has zero test coverage; add view history, view single, cancel | `tests/Feature/Api/V1/OrderApiTest.php` (new) | M |
| 14 | **Write tests for Notification endpoints** — `NotificationController` zero coverage | `tests/Feature/Api/V1/NotificationApiTest.php` (new) | S |
| 15 | **Write tests for Wishlist endpoints** — `WishlistController` zero coverage | `tests/Feature/Api/V1/WishlistApiTest.php` (new) | S |
| 16 | **Write tests for Product Reviews** — `ProductReviewController` zero coverage | `tests/Feature/Api/V1/ReviewApiTest.php` (new) | S |
| 17 | **Write tests for file upload endpoints** — avatar upload, product image upload; validate mime/size rejection | add to existing test files | M |
| 18 | **Add order status / payment status filter to `OrderController::index()`** — `CheckoutService::orderHistory()` returns all orders with no filtering; buyers cannot filter by status, date range, or order number | `app/Services/CheckoutService.php:173` | S |
| 19 | **Add a `StoreProductRequest` minimum price of 1** — `price_amount` currently allows 0; free listings break payment math | `app/Http/Requests/Product/StoreProductRequest.php:28` | XS |
| 20 | **Complete API_ENDPOINTS_DOCUMENTATION.md** — document covers only Auth, Addresses, Products, Profile; missing Commerce, Chat, Social, Admin, AI sections | `API_ENDPOINTS_DOCUMENTATION.md` | M |
| 21 | **Remove `resources/views/pages/welcome.blade.php`** — legacy Laravel onboarding page; not linked from any route or layout; creates confusion | `resources/views/pages/welcome.blade.php` | XS |
| 22 | **Enable TikTok integration** — social accounts page shows TikTok as "Coming soon"; OAuth flow, token management, and posting service need completion | `app/Services/SocialPlatformManager.php`, `routes/api/v1/social.php` | L |
| 23 | **Add refund / dispute mechanism** — no route, controller, or model for refunds; orders can only be cancelled before payment | new migration, model, controller, route | XL |

---

## PRIORITY 4 — LOW (polish & maintenance)

| # | Task | File(s) | Effort |
|---|------|---------|--------|
| 24 | **Capture request body in `LogApiActivity` middleware** — currently logs method, path, IP, and status only; full audit trail for compliance needs sanitised request/response body | `app/Http/Middleware/LogApiActivity.php` | S |
| 25 | **Refactor `CartController` inline `abort_unless` to policy-based authorization** — inconsistent with rest of codebase which uses `$this->authorize()` | `app/Http/Controllers/Api/V1/CartController.php:46,58` | XS |
| 26 | **Add max length to `LoginRequest` email and password fields** — currently unbounded; add `max:255` for email and `max:1024` for password | `app/Http/Requests/Auth/LoginRequest.php` | XS |
| 27 | **Add max length to `ConnectSocialAccountRequest` token fields** — `access_token` and `refresh_token` are unbounded strings | `app/Http/Requests/Social/ConnectSocialAccountRequest.php` | XS |
| 28 | **Complete i18n key coverage** — some Blade views may use untranslated `data-i18n` keys; audit and add missing Khmer/English keys to `resources/js/i18n.js` | `resources/js/i18n.js` | M |
| 29 | **Add skeleton loading screens** — many pages show plain text "Loading…"; replace with Tailwind skeleton placeholders for better UX | `resources/views/pages/*.blade.php` | M |
| 30 | **Rotate / regenerate credentials before any public deploy** — `.env` contains real Facebook/TikTok OAuth secrets and a JWT secret; these should be rotated before any shared or cloud environment | `.env` | XS |
